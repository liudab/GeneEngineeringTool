- IPC 通道名统一以 `db:` 或 `file:` 前缀命名，集中定义在 shared/types.ts 的 IPC_CHANNELS 常量对象中，main 与 preload 均通过该常量引用，避免硬编码字符串。
- 每个业务域（enzyme/vector/gene/lab-vector）在 ipc.ts 中按 LIST/GET/CREATE/UPDATE/DELETE 五个动词成组注册 ipcMain.handle，形成一致的 CRUD 通道约定。
- database.ts 中的更新操作采用 Partial<T> 参数 + fields/values 动态拼接 SQL 的模式，仅写入非 undefined 字段，保持 update* 方法结构一致。
- 主进程对渲染进程的暴露严格遵循白名单：preload 只将必要的函数挂到 window.api，不直接暴露 ipcRenderer 或 Node API，配合 contextIsolation=true 实现最小权限原则。
- TypeScript 路径别名统一配置：@shared → src/shared（供 main/preload/renderer 共享类型），@ → src/renderer（供组件内部相对导入）。